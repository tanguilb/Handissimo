//$(document).ready(function () {
  //  $(function () {
    /*$("#progressbar").progressbar({
            value: 0,
            max: 100,
            //textFormat: 'fraction',
            change: function () {
                var val = $("#progressbar").progressbar( "value" ) || 0;
                $("#percentage").text( $("#progressbar").progressbar( "value" ) + "%" )
            },
            complete: function(event, ui) {
                alert("Bordel!");
            }
        });

        $(".stat").change(function() {
            var pbVal = 0;
            if ($("#handissimobundle_organizations_name").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_structureLogo").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_address").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_postal").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_city").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_phone_number").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_mail").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_directorName").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_orgaStructure").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_agemini").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_agemaxi").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_freeplace").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_interventionZone").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_teamMembersNumber").val().length > 0) pbVal += 5;
            if ($("#handissimobundle_organizations_openhours").val().length > 0) pbVal += 5;
            //var editor_val = CKEDITOR.instances.handissimobundle_organizations_organizationDescription.document.getBody().getChild(0).getText();
            if (!editorEmpty(handissimobundle_organizations_organizationDescription)) pbVal += 5;
            $( "#progressbar" ).progressbar( "option", "value", pbVal );
            return false;
        });
    });*/
        /*$("#handissimobundle_organizations_disabilitytypes").on('click');
        if ($("#handissimobundle_organizations_disabilitytypes:checked").length > 0) {
            pbVal += 5;
        }else{
            pbVal -= 5;
        }*/


            //if (CKEDITOR.instances.cke_handissimobundle_organizations_organizationDescription.getData() !== "") pbVal += 5;
            //if ($("#handissimobundle_organizations_needs").val().length > 0) pbVal += 20;
            //if ($("#handissimobundle_organizations_secondneeds").val().length > 0) pbVal += 20;
            //if ($("#cke_handissimobundle_organizations_workingDescription").val().length > 0) pbVal += 5;
            //if ($("#handissimobundle_organizations_accomodation").val().length > 0) pbVal += 20;
            //if ($("#cke_handissimobundle_organizations_accomodationDescription").val().length > 0) pbVal += 5;
            //if ($("#handissimobundle_organizations_school").val().length > 0) pbVal += 20;
            //if ($("#cke_handissimobundle_organizations_schoolDescription").val().length > 0) pbVal += 5;
            //if ($("#cke_handissimobundle_organizations_dayDescription").val().length > 0) pbVal += 5;
            //if ($("#cke_handissimobundle_organizations_receptionDescription").val().length > 0) pbVal += 5;
            //if ($("#handissimobundle_organizations_stafforganizations").val().length > 0) pbVal += 20;
            //if ($("#handissimobundle_organizations_socialstaffs").val().length > 0) pbVal += 20;
            //if ($("#handissimobundle_organizations_otherjobs").val().length > 0) pbVal += 20;
            //if ($("#handissimobundle_organizations_opendays").val().length > 0) pbVal += 20;
            //if ($("#handissimobundle_organizations_opendaysYear").val().length > 0) pbVal += 20;
            //if ($("#cke_handissimobundle_organizations_inscription").val().length > 0) pbVal += 5;
            //if ($("#handissimobundle_organizations_orientationMdph").val().length > 0) pbVal += 20;
            //if ($("#cke_handissimobundle_organizations_cost").val().length > 0) pbVal += 5;
            //if ($("#cke_handissimobundle_organizations_transport").val().length > 0) pbVal += 5;

        /*$(".cke").change(function() {
            var pbVal = 0;
            if (isBlanck(CKEDITOR.instances.handissimobundle_organizations[organizationDescription].getData())) pbVal += 5;
            $( "#progressbar" ).progressbar( "option", "value", pbVal );
            return false;
        })*/


  /*  function editorEmpty(instanceName){

        //var ele = (new Element('div')).update(CKEDITOR.instances[instanceName].getData());
        var ele = (new Element('div')).update(CKEDITOR.instances[instanceName].document.getBody().getChild(0).getText());

        return (ele.getInnerText() == '' || innerText.search(/^(&nbsp;)+$/i) == 0);
    }
});*/

