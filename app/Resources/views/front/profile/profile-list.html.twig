{% extends '@SonataUser/Profile/action.html.twig' %}

{% block sonata_profile_content %}
    <div class="title-block">
        <h3 class="title-search">Dernières fiches créées</h3>
    </div>
    <div class="block-profil-list">
        <div class="col-md-4 title-column">Nom de la structure</div>
        <div class="col-md-4 title-column">Dernière mise à jour</div>
        <div class="col-md-2 title-column">Relecture</div>
        <div class="col-md-2 title-column">Validation</div>
        {% for results in pagination %}
            <div class="col-md-4">
                <a href="{{ path('handissimo_profile_version', {'id': results.id}) }}" class="link-about"
                   data-toggle="tooltip" data-placement="bottom" title="{{ results.name }}">
                    {{ results.name|length > 30 ? results.name|slice(0,30)~'...' : results.name }}
                </a>
            </div>
            <div class="col-md-4">
                {{ results.updateDatetime|localizeddate }}
            </div>
            <div class="col-md-2">
                {% if results.replay %}
                    <p class="yes">Oui</p>
                {% else %}
                    <p class="no">Non</p>
                {% endif %}
            </div>
            <div class="col-md-2">
                {% if results.statut %}
                    <p class="yes">Oui</p>
                {% else %}
                    <p class="no">Non</p>
                {% endif %}
            </div>
        {% endfor %}
    </div>
    <div class="col-xs-12">
        <div class="row navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
    <div class="col-xs-12">
        <h4>Archivage des fiches supprimées</h4>
        <a href="{{ path('handissimo_profile_list_deletion') }}" class="link-about">ICI</a>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/popover.js') }}"></script>
{% endblock %}